# HTTPS 적용 후 사용 (deploy/vps/setup-https.sh)

services:
  frontend:
    volumes:
      - certbot-www:/var/www/certbot
      - certbot-conf:/etc/letsencrypt:ro
      - ./deploy/vps/generated/nginx.ssl.conf:/etc/nginx/conf.d/default.conf:ro

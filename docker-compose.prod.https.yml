# @deprecated — frontend docker-entrypoint.sh 가 인증서를 감지해 SSL nginx 를 설정합니다.
# 이 파일은 하위 호환용이며, default.conf 를 마운트하지 않습니다.

services:
  frontend:
    volumes:
      - certbot-www:/var/www/certbot
      - certbot-conf:/etc/letsencrypt:ro

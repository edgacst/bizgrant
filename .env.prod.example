# VPS에 복사 후 .env 로 저장
# cp .env.prod.example .env && nano .env

# ---- 필수 ----
POSTGRES_PASSWORD="BzGr2026xK9mP4qL7nR2"
JWT_SECRET="bizgrant-jwt-secret-2026-srv1757686-hostinger"
PUBLIC_DATA_API_KEY=4kpciVHyniZJV%2BUGGyjbU80aUlB1VqT2BHBRMimmTdySD9O89LhD%2B3JYi%2FpZieBhoDnf5NjvrpBEjIK05TfNpA%3D%3D
SITE_URL=http://187.127.107.200

# ---- 도메인 · HTTPS (setup-https.sh) ----
DOMAIN=bizgrant.kr
CERTBOT_EMAIL=admin@bizgrant.kr
HTTPS_PORT=443

# ---- DB ----
POSTGRES_DB=bizgrant
POSTGRES_USER=postgres

# ---- 웹 포트 ----
HTTP_PORT=80

# ---- 뉴스레터 (SMTP 비워두면 발송만 실패, 사이트는 동작) ----
NEWSLETTER_ENABLED=true
NEWSLETTER_FROM=noreply@bizgrant.kr
MAIL_HOST=
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SMTP_AUTH=false
MAIL_SMTP_STARTTLS=false

# ---- 알림 채널 (SMS·카카오 알림톡: solapi.com 가입 후 발신번호 등록) ----
# SOLAPI_API_KEY=
# SOLAPI_API_SECRET=
# SOLAPI_SENDER=01012345678
# KAKAO_PF_ID=
# KAKAO_TEMPLATE_ID=
# KAKAO_CONTENT_VARIABLE=#{내용}

# ---- Telegram (BotFather에서 봇 생성 후 토큰) ----
# TELEGRAM_BOT_TOKEN=

# ---- 관리자 (최초 로그인, 생략 시 BizGrant2026!) ----
# ADMIN_PASSWORD=BizGrant2026!

# ---- JVM (Hostinger KVM 1: 4GB RAM 기준) ----
JAVA_OPTS="-Xms256m -Xmx512m -XX:+UseG1GC"

# ---- 동기화 (매일 새벽 3시) ----
GRANT_SYNC_CRON="0 0 3 * * *"

# ---- 검색엔진 소유 확인 (Search Console·네이버 웹마스터에서 발급 후 프론트 재빌드) ----
# VITE_GOOGLE_SITE_VERIFICATION=
# VITE_NAVER_SITE_VERIFICATION=
# VITE_BING_SITE_VERIFICATION=

# ---- Google Analytics 4 (프론트 재빌드 필요) ----
VITE_GA_MEASUREMENT_ID=G-CQLJP00G2E
